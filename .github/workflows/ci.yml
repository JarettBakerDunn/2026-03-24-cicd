on:
  push:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello World
        run: echo "Hello World"
      - name: Second Step
        run: echo "Second Step"

  goodbye:
    runs-on: ubuntu-latest
    needs: hello
    steps:
      - name: Print Goodbye
        run: echo "Goodbye"
